# Runs

Local runs created by `tools/run_eval.py` default to `runs/local/`, which is ignored by git.

Reviewed community or maintainer runs can be added later in a curated subdirectory, but raw local output should not be committed directly.
