# Examples

These files are reference examples from the original local testing. They are not part of the canonical eval definition.

- `profiles/` contains machine-readable profile cards.
- `launch/` contains example `llama.cpp` launch scripts.

Use them as starting points only. Your local paths, ports, CUDA build, context size, and KV cache choices may differ.
