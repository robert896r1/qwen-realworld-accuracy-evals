# max-accuracy-v1

Exact-scored profile comparison suite. VRAM is intentionally not part of the score. Cases use deterministic JSON fields with known answers, including code review, operator-instruction fidelity, UI regression detection, and long-context needle retrieval.
